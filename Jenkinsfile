pipeline {
   agent any
  
   stages {
      stage('python') {
         steps {
            echo 'this is the python file step'
         }
      }
	  stage('c') {
         steps {
            echo 'this is the c file step'
         }
      }
	  stage('bash') {
         steps {
            echo 'this is the bash file step'
         }
      }
   }
}
